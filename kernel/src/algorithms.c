#include "algorithms.h"

pcb* fifo(pcb *pcb){
  printf("fifo \n");
  //  t_queue* ready_queue = queue_create;
  // pcb.state_pcb = READY;
  return pcb;
}

pcb* hrrn(pcb *pcb){
  printf("hrrn \n");
  //  t_queue* ready_queue = queue_create;
  //  pcb.state_pcb = READY;
  return pcb;
}