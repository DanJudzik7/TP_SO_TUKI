#include "algorithms.h"

void fifo(pcb pcb){
  printf("fifo \n");
  //  t_queue* ready_queue = queue_create;
  // pcb.state_pcb = READY;
  exit(2);
}

void hrrn(pcb pcb){
  printf("hrrn \n");
  //  t_queue* ready_queue = queue_create;
  //  pcb.state_pcb = READY;
  exit(2);
}